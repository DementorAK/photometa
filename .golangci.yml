run:
  timeout: 5m

linters:
  disable-all: false
  enable:
    - errcheck
    - govet
    - staticcheck
    - gosimple
    - revive
    - unconvert
    - goconst

issues:
  max-issues-per-linter: 0
  max-same-issues: 0